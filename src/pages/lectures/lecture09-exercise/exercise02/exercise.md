# 실습 02: 사용자 목록 앱 만들기

## 📝 학습 목표
- `useEffect`를 사용한 생명주기 관리 이해하기
- API 호출과 비동기 데이터 처리 익히기
- 로딩 상태와 에러 처리 구현하기
- 조건부 렌더링 실습하기

## 🎯 요구사항

### 기본 기능
1. **사용자 목록 표시**: 컴포넌트가 마운트될 때 API에서 사용자 데이터 가져오기
2. **로딩 상태**: 데이터를 가져오는 동안 로딩 스피너 또는 메시지 표시
3. **에러 처리**: API 호출 실패 시 에러 메시지 표시
4. **사용자 카드**: 각 사용자의 정보를 카드 형태로 표시
5. **새로고침 버튼**: 버튼 클릭 시 데이터 다시 가져오기

### UI 요구사항
- 사용자 카드에 표시할 정보: 이름, 이메일, 전화번호, 웹사이트
- 그리드 레이아웃으로 카드 배치 (반응형)
- 로딩/에러/데이터 상태에 따른 조건부 렌더링
- Tailwind CSS를 사용하여 스타일링

## 🌐 API 정보

**JSONPlaceholder API 사용**
- URL: `https://jsonplaceholder.typicode.com/users`
- Method: GET
- 응답: 10명의 사용자 정보 배열

### 응답 데이터 구조
```json
[
  {
    "id": 1,
    "name": "Leanne Graham",
    "username": "Bret",
    "email": "Sincere@april.biz",
    "phone": "1-770-736-8031 x56442",
    "website": "hildegard.org",
    "address": {
      "street": "Kulas Light",
      "city": "Gwenborough"
    },
    "company": {
      "name": "Romaguera-Crona"
    }
  },
  ...
]
```

## 🏗️ 컴포넌트 구조 예시

```
UserListApp (메인 컴포넌트)
├─ LoadingSpinner (로딩 컴포넌트)
├─ ErrorMessage (에러 컴포넌트)
└─ UserCard (사용자 카드 컴포넌트)
```

## 💡 힌트

### 1. User 타입 정의
```typescript
interface User {
  id: number;
  name: string;
  email: string;
  phone: string;
  website: string;
  address: {
    city: string;
  };
  company: {
    name: string;
  };
}
```

### 2. 사용할 Hook과 State
```typescript
const [users, setUsers] = React.useState<User[]>([]);
const [loading, setLoading] = React.useState<boolean>(true);
const [error, setError] = React.useState<string>('');
```

### 3. useEffect로 데이터 가져오기
```typescript
React.useEffect(() => {
  const fetchUsers = async () => {
    try {
      setLoading(true);
      setError('');
      const response = await fetch('https://jsonplaceholder.typicode.com/users');
      
      if (!response.ok) {
        throw new Error('데이터를 불러오는데 실패했습니다.');
      }
      
      const data = await response.json();
      setUsers(data);
    } catch (err) {
      setError(err instanceof Error ? err.message : '알 수 없는 오류가 발생했습니다.');
    } finally {
      setLoading(false);
    }
  };
  
  fetchUsers();
}, []); // 빈 배열: 컴포넌트 마운트 시 한 번만 실행
```

### 4. 조건부 렌더링
```typescript
if (loading) {
  return <LoadingSpinner />;
}

if (error) {
  return <ErrorMessage message={error} onRetry={fetchUsers} />;
}

return (
  <div>
    {/* 사용자 목록 표시 */}
  </div>
);
```

## 📋 체크리스트

기본 요구사항:
- [ ] 컴포넌트 마운트 시 API에서 사용자 데이터를 가져온다
- [ ] 데이터 로딩 중에는 로딩 UI가 표시된다
- [ ] API 호출 실패 시 에러 메시지가 표시된다
- [ ] 사용자 정보가 카드 형태로 표시된다
- [ ] 새로고침 버튼을 클릭하면 데이터를 다시 가져온다
- [ ] 그리드 레이아웃으로 카드가 배치된다 (md:grid-cols-2, lg:grid-cols-3)

추가 도전 과제:
- [ ] 검색 기능 추가 (이름 또는 이메일로 필터링)
- [ ] 정렬 기능 추가 (이름순, 이메일순)
- [ ] 사용자 카드 클릭 시 상세 정보 모달 표시
- [ ] 페이지네이션 또는 무한 스크롤 구현
- [ ] 사용자 아바타 이미지 추가 (예: `https://i.pravatar.cc/150?img=${user.id}`)

## 🎨 UI 참고 예시

```
┌────────────────────────────────────────────────┐
│  👥 User Directory         [🔄 새로고침]       │
├────────────────────────────────────────────────┤
│  ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 👤       │ │ 👤       │ │ 👤       │       │
│  │ Name     │ │ Name     │ │ Name     │       │
│  │ Email    │ │ Email    │ │ Email    │       │
│  │ Phone    │ │ Phone    │ │ Phone    │       │
│  │ Website  │ │ Website  │ │ Website  │       │
│  └──────────┘ └──────────┘ └──────────┘       │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 👤       │ │ 👤       │ │ 👤       │       │
│  │ ...      │ │ ...      │ │ ...      │       │
│  └──────────┘ └──────────┘ └──────────┘       │
└────────────────────────────────────────────────┘
```

## 📚 관련 학습 내용
- Lecture 09: useEffect
- Lecture 07: 조건부 렌더링
- Lecture 18: 이벤트 핸들링

## 💪 도전해보세요!
이 실습을 통해 React의 생명주기와 비동기 데이터 처리, 그리고 실제 API 연동 방법을 익힐 수 있습니다.

## 🔍 디버깅 팁
- 브라우저 개발자 도구의 Network 탭에서 API 요청 확인
- console.log로 데이터 구조 확인
- try-catch로 에러를 잡아 console.error로 출력
- React DevTools로 state 변화 관찰

